# Tu programa
if(button != ''): sql(button) #Si el botón está vacío no se va a ejecutar nada
# Tu programa


def call_sql(b):
    while(True):
        db.execute("whatevergoeshere... {0}.jpg ... whateverhastalascomillas".format(b))
        # El resto del código este que tenés la llamada a SQL
        break
